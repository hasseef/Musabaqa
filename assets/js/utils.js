export const fmtDate=d=>new Date(d).toLocaleDateString('ar-SA',{year:'numeric',month:'short',day:'2-digit'}); export const uid=()=>Math.random().toString(36).slice(2); export const clamp=(n,min,max)=>Math.max(min,Math.min(max,n)); export const pct=(a,b)=>b?Math.round((a/b)*100):0; export const downloadFile=(fn,txt)=>{const b=new Blob([txt],{type:'text/plain;charset=utf-8'});const u=URL.createObjectURL(b);const a=document.createElement('a');a.href=u;a.download=fn;a.click();URL.revokeObjectURL(u)};